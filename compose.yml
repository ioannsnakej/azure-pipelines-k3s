services:
  hello-app:
    build: .
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http:/localhost:5000/books"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
